// Variable without a value will show undefined (no value)
var name;
console.log(name);

// Assign value to a variable with no value
name = "developer";
console.log(name);

// Assign value to variable
var age = 200;
console.log(age);

// null is "explicitly nothing" (will print out 'null')
var player = null;
console.log(player);
